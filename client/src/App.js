import SignIn from './pages/SignIn';
import { ThemeProvider } from '@mui/material';
import theme from './Theme/theme'

function App() { // Eventually think of some home page for the workout tracker
  return (
      <SignIn />
  );
}

export default App;
